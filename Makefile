# Partner 2 drafts — see detailed plan for reference
# Requirements: FLAGS = -Wall -Wextra -g, PORT variable with default 4242,
# 'all' as first rule, explicit .o dependency rules, gcc directly, ${FLAGS} syntax
